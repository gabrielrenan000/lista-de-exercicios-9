#include <stdio.h>

int main() {

    int tab, cont, result;
cont= 1;
tab = 5;
while (cont <=10)

{
        result = tab * cont;
        printf("%d x %d = %d", tab, cont, result);
    printf("\n");
        cont = cont + 1;
    }

    return 0;
}
